<?php

    $server = "localhost";
    $username = "root";
    $password = "";
    $db = "vocabulary";

    /* CREATE A CONNECTION */
    $conn = mysqli_connect($server, $username, $password, $db);

?>